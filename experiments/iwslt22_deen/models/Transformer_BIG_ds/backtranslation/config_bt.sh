#############################
# Backtranslations config   #
#############################

# Fill the following fields in the perspective of the model that will use the backtranslation.
# That is, TARGET_LANG_SUFFIX should contain the language code of the monolingual data.
SOURCE_LANG_SUFFIX=src
TARGET_LANG_SUFFIX=tgt

# Fill the following fields
FAIRSEQ=/home/jiranzo/trabajo/git/fairseq/fairseq-0.9.0-efficient-simultaneous/
PYTHON_ENV=/home/"$USER"/trabajo/env/venv_LATEST_fairseq/bin/activate

# Fill the following fields
# Number of phrases to include in each split of the monolingual data (for parallelization)
SPLIT_LENGTH=50000
MONO_PATH=/path/to/monolingual/corpora/file
PREPRO_MODELS_PATH=/path/to/dir/containing/preprocessing/models
# This model path must contain the translation model to use for the BT
# as well as the dict.{lang}.txt files generated by fairseq-preprocess
BT_MODEL=/path/to/dir/containing/model/for/backtranslation

# Obtain (rounded) max suffix for job arrays
max_split_suffix=$((( $(wc -l $MONO_PATH | cut -f1 -d " ") + ($SPLIT_LENGTH - 1)) / $SPLIT_LENGTH))

##########################
# Other utils config     #
##########################
moses_scripts=/scratch/jiranzo/trabajo/git/mosesdecoder/scripts
SUBWORD_FOLDER=/home/jiranzo/trabajo/git/subword-nmt

